## To do List

- [x] Fix: Error on handling code integration on blank project
	- [x] On root asset (project `.yyp`)
	- [x] Scripts' / Objects' `.yy`
- [x] Feat: Add `source` option in `scaff.config.js|ts`
- [x] Feat!: Remove `source` args and use `source` option in `scaff.config.js|ts` instead
- [x] Feat!: Add `--integrate` and `--no-integration` flags to `scaff generate` to integrate the generated source code
- [x] Feat!: Add root (`~`) support on path aliases to get the module from `source` option
- [x] Fix: Uncompiled arrow functions in `impl`
- [x] Fix: Class not found if `impl` is not under the same directory as the targetted class
- [x] Feat: Ignore directories and files that start with `_`.
- [x] Feat: Wildcard path aliases.

